"""
scripts/capture.py — agent de capture en mode terminal (sans tray).

Usage direct :
    uv run scripts/capture.py

Usage recommandé (tout-en-un) :
    uv run run.py
"""

from agent.cli import main

if __name__ == "__main__":
    main()
